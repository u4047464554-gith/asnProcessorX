name: CI - Test & Lint

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  backend-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # Install local asn1tools first (it's in sources folder)
          pip install -e sources/asn1tools
          # Install remaining requirements (skip the -e line as we already installed it)
          grep -v "^-e " backend/requirements.txt | pip install -r /dev/stdin
          pip install ruff mypy pytest pytest-cov pytest-asyncio pytest-mock httpx
          
      - name: Lint with Ruff
        run: ruff check backend/
        
      - name: Type check with MyPy
        run: mypy backend --ignore-missing-imports || true  # Allow failure for now as we fix types
        
      - name: Run Backend Tests
        run: pytest backend/tests/ -v --cov=backend

  frontend-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
          
      - name: Install dependencies
        working-directory: ./frontend
        run: npm ci
        
      - name: Lint
        working-directory: ./frontend
        run: npm run lint
        
      - name: Run Tests
        working-directory: ./frontend
        run: npm run test
        
      - name: Build Check
        working-directory: ./frontend
        run: npm run build
